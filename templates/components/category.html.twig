<a href="{{ path('app_category_show', { id: category.id }) }}">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{ category.label }}</h5>
        </div>
    </div>
</a>