{% extends 'base.html.twig' %}

{% block title %}Livres{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            {% for book in books %}
                <div class="col-sm-4 mb-3 mb-sm-0">
                    {% include 'components/book.html.twig' with { book } only %}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
